# Legacy Prototype

This folder is retained only as a reference for the early Synqora product flow and visual exploration.

Do not add new product functionality here.

Active implementation targets:

- Go API: `apps/api-go`
- React UI: `apps/web-react`
- Python migration engine: `apps/engine-python`

The old static HTML/CSS/JavaScript prototype should be retired after the React UI reaches feature parity.
